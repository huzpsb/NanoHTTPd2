This is a list of where the files in this folder are used in the code.
Hopefully this will help you to understand the code better.

- `MANIFEST.MF` is read by the JVM. It decides which classes to load as the main class.
- `favicon.ico` is used by nano.http.d2.hooks.impls.DefaultRequest , demonstrating how to serve a file in hook.